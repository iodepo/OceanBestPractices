import { Construct } from "@aws-cdk/core";
import {} from '@aws-cdk/aws-ec2';

export default class Virtuoso extends Construct {
  constructor(scope: Construct, id: string, props: unknown) {
    super(scope, id);

    
  }
}
