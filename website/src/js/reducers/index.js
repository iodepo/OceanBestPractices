import searchReducer from './search';
import statsReducer from './stats';
import taggerReducer from './tagger';
import fields from './fields';
import options from './options';

const reducers = {
  searchReducer,
  tagsReducer,
  statsReducer,
  taggerReducer,
  fields,
  options,
}

export default reducers;
